# Exact-byte retention note

`AzureCdnInfo.edrtester@1.0.4` is excluded from the publishable accuracy
holdout because the cited Marketplace artifact is not retained locally and the
external endpoint did not provide a promptly verifiable exact byte stream. The
independent report and hash remain research inputs, but this release does not
claim to have scanned that exact sample. Re-add it only after the reviewed VSIX
is stored in the private artifact vault.
